# 로그인 시스템 테스트 가이드

이 가이드는 새로 구현된 로그인 시스템을 로컬 환경에서 테스트하는 방법을 안내합니다.

## 📋 사전 준비

### 1. 환경 변수 설정

#### Backend (`backend/.env`)

```bash
# 필수 암호화 키 생성
python -c "import secrets, base64; print('ENCRYPTION_MASTER_KEY=' + base64.b64encode(secrets.token_bytes(32)).decode())"
python -c "import secrets, base64; print('ENCRYPTION_SALT=' + base64.b64encode(secrets.token_bytes(16)).decode())"
python -c "import secrets; print('JWT_SECRET_KEY=' + secrets.token_urlsafe(64))"
```

생성된 값을 `backend/.env` 파일에 추가:

```bash
ENCRYPTION_MASTER_KEY=<생성된_키>
ENCRYPTION_SALT=<생성된_솔트>
JWT_SECRET_KEY=<생성된_시크릿>
LULU_LALA_USERNAME=<실제_룰루랄라_아이디>
LULU_LALA_PASSWORD=<실제_룰루랄라_비밀번호>
```

#### Frontend (`frontend/.env.local`)

```bash
NEXT_PUBLIC_API_URL=http://localhost:8000
```

### 2. 데이터베이스 마이그레이션

```bash
cd backend
alembic upgrade head
```

---

## 🧪 백엔드 테스트

### 1. 서버 실행

```bash
cd backend
source venv/bin/activate  # Windows: venv\Scripts\activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

서버가 정상적으로 실행되면 http://localhost:8000/docs 에서 Swagger UI를 확인할 수 있습니다.

### 2. API 테스트 (cURL)

#### 2.1 로그인 테스트

```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "<룰루랄라_아이디>",
    "password": "<룰루랄라_비밀번호>"
  }'
```

**예상 응답** (8-10초 소요):
```json
{
  "access_token": "eyJ...",
  "refresh_token": "eyJ...",
  "token_type": "bearer",
  "user": {
    "id": "user-uuid",
    "name": "사용자명",
    "lulu_lala_user_id": "룰루랄라ID",
    "points": 100,
    "available_nights": 10,
    "is_verified": true
  }
}
```

**체크포인트**:
- ✅ 응답 시간 8-10초 이내
- ✅ access_token, refresh_token 반환
- ✅ user 정보 포함
- ✅ is_verified: true

#### 2.2 사용자 정보 조회

```bash
# 위에서 받은 access_token을 사용
curl -X GET http://localhost:8000/api/auth/me \
  -H "Authorization: Bearer <access_token>"
```

**예상 응답**:
```json
{
  "user": {
    "id": "user-uuid",
    "name": "사용자명",
    ...
  },
  "session_status": "active"
}
```

**체크포인트**:
- ✅ 사용자 정보 정상 반환
- ✅ session_status: "active"

#### 2.3 토큰 갱신 테스트

```bash
curl -X POST http://localhost:8000/api/auth/refresh \
  -H "Content-Type: application/json" \
  -d '{
    "refresh_token": "<refresh_token>"
  }'
```

**예상 응답**:
```json
{
  "access_token": "eyJ...",
  "token_type": "bearer"
}
```

**체크포인트**:
- ✅ 새 access_token 발급
- ✅ 응답 시간 < 100ms

#### 2.4 로그아웃 테스트

```bash
curl -X POST http://localhost:8000/api/auth/logout \
  -H "Authorization: Bearer <access_token>"
```

**예상 응답**:
```json
{
  "message": "Logged out successfully"
}
```

**체크포인트**:
- ✅ 로그아웃 성공
- ✅ 이후 같은 토큰으로 /api/auth/me 호출 시 401 에러

### 3. 데이터베이스 확인

```bash
# SQLite 사용 시
sqlite3 refresh_plus.db "SELECT id, lulu_lala_user_id, is_verified FROM users;"
```

**체크포인트**:
- ✅ 사용자 레코드 생성됨
- ✅ `lulu_lala_user_id` 저장됨
- ✅ `encrypted_password` 저장됨 (바이너리 데이터)
- ✅ `is_verified` = 1 (True)

---

## 🎨 프론트엔드 테스트

### 1. 서버 실행

```bash
cd frontend
npm run dev
```

서버가 http://localhost:3000 에서 실행됩니다.

### 2. UI 플로우 테스트

#### 2.1 로그인 플로우

**테스트 시나리오**:

1. **홈페이지 접속** (`http://localhost:3000`)
   - ✅ 페이지 정상 로드

2. **비로그인 상태에서 보호된 페이지 접근**
   - 하단 네비게이션에서 "MY숙소" 또는 "즐겨찾기" 클릭
   - ✅ 자동으로 `/login?redirect=/my` (또는 `/wishlist`)로 리다이렉트

3. **로그인 페이지 - 웰컴 스크린**
   - ✅ SOL 캐릭터 이미지 표시
   - ✅ "Refresh Plus" 타이틀
   - ✅ 3가지 기능 소개 카드
   - ✅ "룰루랄라 계정으로 로그인" 버튼

4. **로그인 페이지 - 자격증명 입력**
   - "룰루랄라 계정으로 로그인" 버튼 클릭
   - ✅ 아이디/비밀번호 입력 폼 표시
   - ✅ 보안 안내 박스 표시

5. **로그인 시도**
   - 룰루랄라 아이디와 비밀번호 입력
   - "로그인" 버튼 클릭
   - ✅ "로그인 중... (8-10초 소요)" 메시지 표시
   - ✅ 로딩 스피너 애니메이션
   - ✅ 버튼 비활성화

6. **로그인 성공**
   - ✅ 원래 접근하려던 페이지로 자동 리다이렉트 (`/my` 또는 `/wishlist`)
   - ✅ 사용자 정보 표시 (이름, 포인트)
   - ✅ localStorage에 토큰 저장 확인 (개발자 도구)

#### 2.2 로그인 후 기능 테스트

**MY숙소 페이지** (`/my`):

1. **사용자 정보 카드**
   - ✅ 사용자 이름 표시: "안녕하세요, {name}"
   - ✅ 인증 상태: "인증완료" 또는 "대기중"

2. **포인트 현황 카드**
   - ✅ 사용 가능 포인트 표시
   - ✅ 남은 박수 표시

3. **찜·알림 관리 카드**
   - ✅ 즐겨찾기 개수 표시
   - ✅ "바로가기" 링크 작동

4. **예약 일정 섹션**
   - ✅ 실제 예약 데이터 로드 (WON/PENDING)
   - ✅ 예약이 없으면 "예약 일정이 없습니다" 메시지
   - ✅ 각 예약의 숙소명, 날짜, 상태 표시

5. **신청 내역 카드**
   - ✅ 최근 90일 통계 표시
   - ✅ 승인/대기/미당첨 개수 정확

**즐겨찾기 페이지** (`/wishlist`):

1. **헤더**
   - ✅ 찜한 개수 배지: "{count}곳 찜"

2. **찜한 숙소 목록**
   - ✅ 실제 찜한 숙소 데이터 표시
   - ✅ 숙소명, 위치, 평균 당첨 점수
   - ✅ 알림 ON/OFF 상태
   - ✅ 태그 표시

3. **찜 제거 기능**
   - X 버튼 클릭
   - ✅ 목록에서 즉시 제거
   - ✅ 배지 개수 업데이트

#### 2.3 토큰 갱신 테스트

**테스트 방법**:

1. 개발자 도구 > Application > Local Storage에서 `access_token` 삭제
2. MY숙소 또는 즐겨찾기 페이지에서 API 호출 발생시키기 (새로고침)
3. Network 탭에서 확인:
   - ✅ 첫 요청: 401 Unauthorized
   - ✅ `/api/auth/refresh` 자동 호출
   - ✅ 재시도 요청: 200 OK
   - ✅ 새 토큰으로 localStorage 업데이트

#### 2.4 로그아웃 테스트

**헤더에 로그아웃 버튼이 있다면**:

1. 로그아웃 버튼 클릭
2. ✅ `/login` 페이지로 리다이렉트
3. ✅ localStorage에서 토큰 제거
4. ✅ 다시 보호된 페이지 접근 시 로그인 필요

---

## 🔍 개발자 도구 체크리스트

### Console 확인

- ❌ 에러 메시지 없음
- ❌ 경고 메시지 없음 (중요한 것만)

### Network 탭

**로그인 플로우**:
1. `POST /api/auth/login` (8-10초)
2. `GET /api/auth/me` (< 500ms)
3. `GET /api/bookings` 또는 `/api/wishlist` (< 1초)

**토큰 갱신 플로우** (401 발생 시):
1. `GET /api/...` → 401
2. `POST /api/auth/refresh` (< 100ms)
3. `GET /api/...` (재시도) → 200

### Application > Local Storage

```
http://localhost:3000
├── access_token: "eyJ..."
└── refresh_token: "eyJ..."
```

### Application > Cookies

```
http://localhost:8000
├── access_token (httpOnly, Secure)
└── refresh_token (httpOnly, Secure)
```

---

## ❌ 에러 시나리오 테스트

### 1. 잘못된 비밀번호

**입력**: 틀린 비밀번호
**예상**:
- ✅ 에러 메시지 표시: "로그인에 실패했습니다..."
- ✅ 로딩 상태 해제
- ✅ 다시 입력 가능

### 2. 계정 잠금 (5회 실패)

**입력**: 5번 연속 틀린 비밀번호
**예상**:
- ✅ 6번째 시도 시 에러: "계정이 30분간 잠겼습니다"
- ✅ DB에 `locked_until` 시간 저장

### 3. 네트워크 오류

**방법**: 백엔드 서버 중지 후 로그인 시도
**예상**:
- ✅ 에러 메시지: "로그인 중 오류가 발생했습니다..."
- ✅ 재시도 가능

### 4. 만료된 리프레시 토큰

**방법**:
1. DB에서 `refresh_token_expires_at`을 과거로 수정
2. 페이지 새로고침

**예상**:
- ✅ 자동으로 `/login` 페이지로 리다이렉트
- ✅ localStorage 토큰 제거

---

## 📊 성능 체크리스트

| 작업 | 목표 시간 | 실제 측정 |
|------|----------|----------|
| 로그인 (첫 시도) | 8-10초 | ___초 |
| 사용자 정보 조회 (/me) | < 500ms | ___ms |
| 토큰 갱신 | < 100ms | ___ms |
| 예약 목록 로드 | < 1초 | ___ms |
| 찜 목록 로드 | < 1초 | ___ms |

---

## ✅ 전체 체크리스트

### Backend
- [ ] 환경 변수 설정 완료
- [ ] 데이터베이스 마이그레이션 완료
- [ ] 서버 정상 실행 (8000 포트)
- [ ] Swagger UI 접근 가능
- [ ] 로그인 API 성공 (8-10초)
- [ ] 사용자 정보 조회 성공
- [ ] 토큰 갱신 성공
- [ ] 로그아웃 성공
- [ ] DB에 암호화된 비밀번호 저장 확인

### Frontend
- [ ] 환경 변수 설정 완료
- [ ] 서버 정상 실행 (3000 포트)
- [ ] 보호된 페이지 접근 시 로그인 리다이렉트
- [ ] 로그인 웰컴 스크린 정상 표시
- [ ] 로그인 자격증명 폼 정상 표시
- [ ] 로그인 성공 및 원래 페이지로 리다이렉트
- [ ] MY숙소 페이지 실제 데이터 표시
- [ ] 즐겨찾기 페이지 실제 데이터 표시
- [ ] 토큰 자동 갱신 작동
- [ ] 로그아웃 작동
- [ ] localStorage 토큰 저장/삭제 확인

### 보안
- [ ] 비밀번호가 평문으로 저장되지 않음
- [ ] JWT 토큰에 비밀번호 포함되지 않음
- [ ] httpOnly 쿠키 설정 확인
- [ ] 계정 잠금 기능 작동 (5회 실패)
- [ ] CORS 설정 확인

---

## 🐛 문제 해결

### "Module not found" 에러

```bash
# Backend
cd backend
pip install -r requirements.txt

# Frontend
cd frontend
npm install
```

### "Database migration failed"

```bash
cd backend
alembic downgrade -1
alembic upgrade head
```

### "Cannot connect to API"

1. 백엔드 서버가 실행 중인지 확인: `http://localhost:8000/health`
2. CORS 설정 확인: `backend/.env`의 `CORS_ORIGINS`
3. 프론트엔드 API URL 확인: `frontend/.env.local`의 `NEXT_PUBLIC_API_URL`

### "Playwright browser not found"

```bash
cd backend
playwright install chromium
```

---

## 📞 지원

문제가 발생하면:
1. 로그 확인: 백엔드 콘솔, 브라우저 개발자 도구
2. 환경 변수 재확인
3. 서버 재시작
4. 이슈 등록: GitHub Issues

---

**테스트 날짜**: ___________
**테스터**: ___________
**결과**: ⬜ 통과 / ⬜ 실패
**메모**: ___________
