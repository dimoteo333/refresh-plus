{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python batch/run_wishlist_notification_evening.py",
    "restartPolicyType": "NEVER"
  }
}
