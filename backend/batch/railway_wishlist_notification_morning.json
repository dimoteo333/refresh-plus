{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python batch/run_wishlist_notification_morning.py",
    "restartPolicyType": "NEVER"
  }
}
